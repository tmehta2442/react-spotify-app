import {spotify_key} from '../../hidden/keys';

const apiKey = spotify_key;

const Spotify = {
  getAccessToken() {
    if(access_token) {
      return access_token;
    } else {
      
    }
  }
};

export default Spotify;